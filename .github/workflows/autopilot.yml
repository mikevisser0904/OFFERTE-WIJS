name: Autopilot (geld + agent)

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  autopilot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - run: npm ci

      - name: Autopilot run
        id: run
        run: node scripts/autopilot-run.mjs
        continue-on-error: true

      - name: Commit status + health + wachtrij
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/autopilot-status.json public/autopilot-status.json public/health.json data/maarten-wachtrij.json public/maarten-wachtrij.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "autopilot: status $(date -u +%Y-%m-%dT%H:%MZ)"
          git pull --rebase origin main || true
          git push || true

      - name: Summary
        run: |
          echo "## Autopilot" >> $GITHUB_STEP_SUMMARY
          cat data/autopilot-status.json >> $GITHUB_STEP_SUMMARY

      - name: Fail if site unhealthy
        if: steps.run.outcome == 'failure'
        run: exit 1