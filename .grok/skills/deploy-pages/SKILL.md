---
name: deploy-pages
description: Agent deploy-pages — push main triggert GitHub Pages. workflow deploy-pages.yml
---

# Deploy Pages Agent

Na build: `GITHUB_PAGES=true npm run build`, commit, `git push origin main`. Workflow deployt naar Pages.